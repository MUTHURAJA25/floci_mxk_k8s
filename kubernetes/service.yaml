apiVersion: v1
kind: Service

metadata:
  name: fintech-service
  namespace: fintech

spec:
  selector:
    app: fintech-frontend

  ports:
  - protocol: TCP
    port: 80
    targetPort: 5173

  type: LoadBalancer